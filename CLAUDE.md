See README.md, AGENTS.md, and docs/ for project guidance.

Public lists (news, conservation, gallery, music) are newest-first. New markdown needs a `date:`. See `.cursor/rules/newest-first.mdc`.

Before committing: run `npm run lint` (Terraform lint, Astro check, API syntax, Actions secret-safety). See AGENTS.md and `.cursor/rules/lint-before-commit.mdc`.
